package com.havamania.ui.theme

import androidx.compose.ui.graphics.Color

// --- Midnight Premium (Dark Theme) ---
val DeepNavy = Color(0xFF0B0F14)          // Background
val SurfaceNavy = Color(0xFF121821)       // Surface
val CardColor = Color(0xFF1A2230)         // Card
val SkyBluePrimary = Color(0xFF00C2FF)    // Primary
val SkyBlueAccent = Color(0xFF38BDF8)     // Accent
val TextPrimaryDark = Color(0xFFEAF2FF)   // Text Primary
val TextSecondaryDark = Color(0xFFA3B3C9) // Text Secondary
val CardOutline = Color(0xFF1E293B)       // Outline

// --- Light Theme (Clean & Professional) ---
val LightBg = Color(0xFFF5F7FA)           // Background
val LightSurface = Color(0xFFFFFFFF)      // Surface
val LightCard = Color(0xFFEEF2F7)         // Card
val LightPrimary = Color(0xFF0077FF)      // Primary
val LightAccent = Color(0xFF38BDF8)       // Accent
val TextPrimaryLight = Color(0xFF1A1F2B)  // Text Primary
val TextSecondaryLight = Color(0xFF6B7280) // Text Secondary
val LightOutline = Color(0xFFE2E8F0)      // Outline

// --- Spring Theme (Soft Green & Pastel) ---
val SpringBg = Color(0xFFF0FFF4)          // Background
val SpringSurface = Color(0xFFFFFFFF)     // Surface
val SpringCard = Color(0xFFE6F9ED)        // Card
val SpringPrimary = Color(0xFF4ADE80)     // Primary
val SpringAccent = Color(0xFFF472B6)      // Accent
val TextPrimarySpring = Color(0xFF1F2937) // Text Primary
val TextSecondarySpring = Color(0xFF6B7280) // Text Secondary

// --- Summer Theme (Vibrant Turquoise & Yellow) ---
val SummerBg = Color(0xFFE0F7FF)          // Background
val SummerSurface = Color(0xFFFFFFFF)     // Surface
val SummerCard = Color(0xFFD6F4FF)        // Card
val SummerPrimary = Color(0xFF00B4D8)     // Primary
val SummerAccent = Color(0xFFFFD60A)      // Accent
val TextPrimarySummer = Color(0xFF0F172A) // Text Primary
val TextSecondarySummer = Color(0xFF475569) // Text Secondary

// --- Autumn Theme (Warm Orange & Brown) ---
val AutumnBg = Color(0xFFFFF7ED)          // Background
val AutumnSurface = Color(0xFFFFFFFF)     // Surface
val AutumnCard = Color(0xFFFFEAD5)        // Card
val AutumnPrimary = Color(0xFFF97316)     // Primary
val AutumnAccent = Color(0xFFA16207)      // Accent
val TextPrimaryAutumn = Color(0xFF3F2A1D) // Text Primary
val TextSecondaryAutumn = Color(0xFF7C5A3C) // Text Secondary

// --- Winter Theme (Ice Blue & Professional Gray) ---
val WinterBg = Color(0xFFF1F5F9)          // Background (Yeni: #F1F5F9)
val WinterSurface = Color(0xFFFFFFFF)     // Surface (Yeni: #FFFFFF)
val WinterCard = Color(0xFFE2E8F0)        // Card (Yeni: #E2E8F0)
val WinterPrimary = Color(0xFF60A5FA)     // Primary (Yeni: #60A5FA)
val WinterAccent = Color(0xFF1E3A8A)      // Accent (Yeni: #1E3A8A)
val TextPrimaryWinter = Color(0xFF0F172A) // Text Primary (Yeni: #0F172A)
val TextSecondaryWinter = Color(0xFF64748B) // Text Secondary (Yeni: #64748B)

// Shared Accents
val SunnyYellow = Color(0xFFFFD600)
val ErrorRed = Color(0xFFEF4444)
val SuccessGreen = Color(0xFF10B981)
