pluginManagement { includeBuild("../node_modules/@react-native/gradle-plugin") }
plugins { id("com.facebook.react.settings") }
extensions.configure(com.facebook.react.ReactSettingsExtension){ ex -> ex.autolinkLibrariesFromCommand() }

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        google()
        mavenCentral()
        maven { url 'https://www.jitpack.io' }
        // Snapshot sunucusunu engellemek için filtre ekliyoruz
        maven {
            url "https://oss.sonatype.org/content/repositories/snapshots/"
            content {
                // includeNothing() yerine uymayacak bir grup ekleyerek etkisizleştiriyoruz
                includeGroup("com.example.nothing")
            }
        }
        // React Native yerel depoları
        maven { url "$rootDir/../node_modules/react-native/android" }
        maven { url "$rootDir/../node_modules/jsc-android/dist" }
    }
}

rootProject.name = 'havamania'
include ':app'
includeBuild('../node_modules/@react-native/gradle-plugin')
