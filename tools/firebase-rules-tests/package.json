{
  "name": "havamania-local-rules-tests",
  "private": true,
  "scripts": {
    "pretest": "node prepare.mjs",
    "test": "firebase emulators:exec --config firebase.json --project demo-havamania-audit --only firestore,storage \"node --test rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "4.0.1",
    "firebase": "11.10.0",
    "firebase-tools": "14.12.0"
  }
}
